[project]
name = "project2"
version = "0.1.0"
description = "AI Resume Analyzer"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "streamlit",
    "pypdf2",
    "python-dotenv",
    "groq"
]
